[package]
name = "mtracker"
version = "0.2.0"
edition = "2024"

[dependencies]
rusqlite = { version = "0.40.1", features = ["bundled"] }

[profile.release]
opt-level = "z"
strip = true
lto = true
codegen-units = 1
panic = "abort"
